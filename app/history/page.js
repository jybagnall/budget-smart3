export default function Page() {
  return <div>status</div>;
}

// Show a log of all recorded items, sortable by date, category, or amount.
// Visualize monthly spending trends, e.g., compare April vs May
